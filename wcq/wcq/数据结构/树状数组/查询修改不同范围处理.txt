1 基于原始数据做bit，则单点修改、区间查询都是 O(logn)
2 基于原始数据的差分数组做bit，则区间修改、单点查询都是 O(logn)。因为
  此时的区间修改仅需要对区间的两个端点分别做一次单点修改  2* O(logn)；
  此时的单点查询是对bit做一次前缀和  O(logn)。
3 树状数组只能维护前缀“操作和”(前缀和，前缀积，前缀最大最小)，而线段
  树可以维护区间操作和
